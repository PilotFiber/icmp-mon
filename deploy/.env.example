# ICMP-Mon Environment Configuration
# Copy this file to .env and fill in actual values
# NEVER commit .env files to git!

# =============================================================================
# DATABASE
# =============================================================================
DB_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# =============================================================================
# 1PASSWORD SERVICE ACCOUNT (Optional - for agent SSH enrollment)
# =============================================================================
# Get token from: https://my.1password.com/integrations/infrastructure-secrets
OP_SERVICE_ACCOUNT_TOKEN=
OP_VAULT=icmp-mon keys

# =============================================================================
# FLIGHT DECK API (Optional - for automatic subnet sync from Pilot)
# =============================================================================
FD_API_URL=https://pilotfiber.com/api/v1/network/resources
FD_BEARER=

# =============================================================================
# DOMAIN (for TLS - Caddy will auto-provision Let's Encrypt cert)
# =============================================================================
DOMAIN=icmpmon-staging.example.com
